Place the packaged backend binary in this directory before running `tauri build`.

Expected filenames:

- Windows: `security-platform-backend.exe`
- macOS/Linux: `security-platform-backend`

Use the root build scripts to produce and copy the backend artifact automatically.

